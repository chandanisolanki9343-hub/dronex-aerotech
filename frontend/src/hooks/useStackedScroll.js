/**
 * Stacked scroll effect has been removed.
 * This hook is kept as a no-op so existing imports don't break.
 */
export function useStackedScroll() {
  // No-op: pinning disabled in favour of free native scroll
}
